/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "f85w27gq4v.ufs.sh",
      // Add any other domains you need
    ],
  },
};

module.exports = nextConfig;
